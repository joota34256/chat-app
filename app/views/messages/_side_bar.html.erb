<div class = "side_header">
  <div class = "user_name">
    <%= link_to current_user.name,edit_user_path(current_user) %>
  </div>
  <div class = "room_create">
    <%= link_to "チャットを作成する",new_room_path %>
  </div>
</div>
<div class = "rooms">
  <%# <% RoomUser.where(user_id:current_user.id).each do |room1|%>
    <%# <div class = "room"> %>
      <%# <div class = "room_name">
        <a href="#"><%= room1.room.name %> 
      <%# </div> %>
    <%# </div> %>
  <%# <% end %> 

  <% current_user.rooms.each do |room| %>
    <div class="room">
      <div class="room-name">
        <%= link_to room.name,room_messages_path(room) %>
      </div>
    </div>
  <% end %>
</div>